## H1 骨のどこか

骨9要素の3つ目「共通ライブラリ読込」は、もう `hello.sh` に書いてあります。
あなたが足すのは最後の要素「ログと終了コード」の1行だけです。

## H2 使う記号か関数名

`log` 関数を使います。呼び方は `log レベル メッセージ` です。
レベルは `INFO` / `OK` / `WARN` / `ERROR` の4つで、この問題では `INFO` を使います。

## H3 ほぼ答えの1行

`log INFO こんにちは`

これだけです。日時も角括弧も自分では書きません。`log` が付けてくれます。
