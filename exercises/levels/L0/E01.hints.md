## H1 骨のどこか

まだスクリプトは書きません。ターミナルに直接3つのコマンドを打ち、そのたびに数字を見るだけです。

## H2 使う記号か関数名

`$?` を使います。`echo "$?"` を、確認したいコマンドの **すぐ次の行** で実行します。
`exit2.sh` の場所は `$LAB_FIXTURES/exit2.sh` です。

## H3 ほぼ答えの1行

`true` は 0、`false` は 1、`exit2.sh` は 2 を返します。
ただし必ず自分で実行し、画面に出た数字を見てから `answer.txt` に書いてください。
